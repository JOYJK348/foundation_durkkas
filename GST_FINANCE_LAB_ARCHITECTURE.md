# 🎨 GST FINANCE LAB - SYSTEM ARCHITECTURE

## 📊 SYSTEM OVERVIEW

```
┌─────────────────────────────────────────────────────────────────────┐
│                     GST FINANCE LAB SYSTEM                          │
│                  Educational Simulation Platform                     │
└─────────────────────────────────────────────────────────────────────┘
                                  │
                    ┌─────────────┴─────────────┐
                    │                           │
            ┌───────▼────────┐         ┌───────▼────────┐
            │   FRONTEND     │         │    BACKEND     │
            │   React UI     │◄───────►│  Next.js API   │
            └────────────────┘         └────────┬───────┘
                                                │
                                       ┌────────▼────────┐
                                       │   PostgreSQL    │
                                       │    Database     │
                                       └─────────────────┘
```

---

## 🔄 DATA FLOW DIAGRAM

### **Complete Transaction Flow**

```
┌──────────────┐
│   STUDENT    │
│  (Frontend)  │
└──────┬───────┘
       │
       │ 1. Register Company
       ▼
┌──────────────────────────────────────────────────────────┐
│  POST /api/ems/practice/student/gst-lab/company          │
│  { company_name, state, state_code }                     │
└──────┬───────────────────────────────────────────────────┘
       │
       │ 2. Service Layer
       ▼
┌──────────────────────────────────────────────────────────┐
│  GSTFinanceLabService.createCompany()                    │
│  → Generate GSTIN                                        │
│  → Insert into gst_lab_companies                         │
└──────┬───────────────────────────────────────────────────┘
       │
       │ 3. Database
       ▼
┌──────────────────────────────────────────────────────────┐
│  INSERT INTO ems.gst_lab_companies                       │
│  RETURNS company with GSTIN: 33XXXXX9999X1Z5            │
└──────┬───────────────────────────────────────────────────┘
       │
       │ 4. Response
       ▼
┌──────────────┐
│   STUDENT    │
│  Dashboard   │
│   Enabled    │
└──────────────┘
```

---

## 🏗️ DATABASE ARCHITECTURE

### **Entity Relationship Diagram**

```
┌─────────────────────────┐
│ student_practice_       │
│   allocations           │
│ ─────────────────────── │
│ • id (PK)              │
│ • student_id           │
│ • module_type: 'GST'   │
└───────────┬─────────────┘
            │ 1
            │
            │ 1
┌───────────▼─────────────┐
│ gst_lab_companies       │
│ ─────────────────────── │
│ • id (PK)              │
│ • allocation_id (FK)   │◄──────────────┐
│ • company_name         │               │
│ • gstin                │               │
│ • state                │               │
└───────────┬─────────────┘               │
            │ 1                           │
            │                             │
      ┌─────┴─────┐                       │
      │           │                       │
      │ 1         │ 1                     │
┌─────▼──────┐  ┌─▼──────────┐            │
│ purchases  │  │   sales    │            │
│ ──────────│  │ ──────────│            │
│ • id (PK) │  │ • id (PK) │            │
│ • company │  │ • company │            │
│ • supplier│  │ • customer│            │
│ • GST amt │  │ • GST amt │            │
└─────┬──────┘  └─┬──────────┘            │
      │           │                       │
      │ Trigger   │ Trigger               │
      │           │                       │
      └─────┬─────┘                       │
            │                             │
            │ N                           │
      ┌─────▼─────────────┐               │
      │ gst_lab_ledgers   │               │
      │ ───────────────── │               │
      │ • id (PK)        │               │
      │ • company_id (FK)│───────────────┘
      │ • ledger_type    │
      │ • balance        │
      └───────────────────┘
            │
            │ Used by
            │
      ┌─────▼─────────────┐
      │ gst_lab_returns   │
      │ ───────────────── │
      │ • id (PK)        │
      │ • company_id (FK)│
      │ • net_tax_payable│
      └─────┬─────────────┘
            │ 1
            │
            │ 1
      ┌─────▼─────────────┐
      │ gst_lab_challans  │
      │ ───────────────── │
      │ • id (PK)        │
      │ • return_id (FK) │
      │ • challan_number │
      └─────┬─────────────┘
            │ 1
            │
            │ 1
      ┌─────▼─────────────┐
      │ gst_lab_payments  │
      │ ───────────────── │
      │ • id (PK)        │
      │ • challan_id (FK)│
      │ • transaction_id │
      └───────────────────┘
```

---

## 🔄 LEDGER UPDATE FLOW

### **Purchase Entry → ITC Update**

```
┌──────────────┐
│  Add Purchase│
│  ₹10,000     │
│  GST: 18%    │
└──────┬───────┘
       │
       │ Calculate GST
       ▼
┌──────────────────┐
│ CGST: ₹900      │
│ SGST: ₹900      │
│ Total: ₹1,800   │
└──────┬───────────┘
       │
       │ Insert into gst_lab_purchases
       ▼
┌──────────────────────────────────┐
│ TRIGGER: trg_purchase_update_itc│
└──────┬───────────────────────────┘
       │
       │ Auto-execute
       ▼
┌──────────────────────────────────┐
│ INSERT INTO gst_lab_ledgers      │
│ ledger_type: INPUT_TAX_CREDIT    │
│ debit_amount: ₹1,800             │
│ balance: previous + ₹1,800       │
└──────┬───────────────────────────┘
       │
       │ Updated
       ▼
┌──────────────────┐
│ ITC Ledger       │
│ Balance: ₹1,800  │
└──────────────────┘
```

### **Sales Entry → Output GST Update**

```
┌──────────────┐
│  Add Sales   │
│  ₹20,000     │
│  GST: 18%    │
└──────┬───────┘
       │
       │ Calculate GST
       ▼
┌──────────────────┐
│ CGST: ₹1,800    │
│ SGST: ₹1,800    │
│ Total: ₹3,600   │
└──────┬───────────┘
       │
       │ Insert into gst_lab_sales
       ▼
┌──────────────────────────────────┐
│ TRIGGER: trg_sales_update_output│
└──────┬───────────────────────────┘
       │
       │ Auto-execute
       ▼
┌──────────────────────────────────┐
│ INSERT INTO gst_lab_ledgers      │
│ ledger_type: OUTPUT_TAX_LIABILITY│
│ credit_amount: ₹3,600            │
│ balance: previous + ₹3,600       │
└──────┬───────────────────────────┘
       │
       │ Updated
       ▼
┌──────────────────┐
│ Output Ledger    │
│ Balance: ₹3,600  │
└──────────────────┘
```

---

## 📅 MONTHLY RETURN FLOW

```
┌─────────────────────┐
│  Month End          │
│  (e.g., Jan 2026)   │
└──────┬──────────────┘
       │
       │ Student clicks "Generate Return"
       ▼
┌─────────────────────────────────────┐
│ GSTFinanceLabService                │
│ .generateMonthlyReturn()            │
└──────┬──────────────────────────────┘
       │
       │ 1. Fetch all purchases for month
       ▼
┌─────────────────────────────────────┐
│ SELECT * FROM gst_lab_purchases     │
│ WHERE invoice_date BETWEEN          │
│   '2026-01-01' AND '2026-01-31'     │
└──────┬──────────────────────────────┘
       │
       │ Total Input GST: ₹5,000
       │
       │ 2. Fetch all sales for month
       ▼
┌─────────────────────────────────────┐
│ SELECT * FROM gst_lab_sales         │
│ WHERE invoice_date BETWEEN          │
│   '2026-01-01' AND '2026-01-31'     │
└──────┬──────────────────────────────┘
       │
       │ Total Output GST: ₹8,000
       │
       │ 3. Calculate Net Tax
       ▼
┌─────────────────────────────────────┐
│ Net Tax Payable =                   │
│   Output GST - Input GST            │
│   ₹8,000 - ₹5,000 = ₹3,000         │
└──────┬──────────────────────────────┘
       │
       │ 4. Create Return Record
       ▼
┌─────────────────────────────────────┐
│ INSERT INTO gst_lab_returns         │
│ • return_month: 'January 2026'      │
│ • output_gst_total: ₹8,000          │
│ • input_gst_total: ₹5,000           │
│ • net_tax_payable: ₹3,000           │
│ • status: 'DRAFT'                   │
└──────┬──────────────────────────────┘
       │
       │ Return Created
       ▼
┌─────────────────────┐
│  Student Reviews    │
│  Return Summary     │
└─────────────────────┘
```

---

## 💳 PAYMENT FLOW

```
┌─────────────────────┐
│  Filed Return       │
│  Net Tax: ₹3,000    │
│  Status: FILED      │
└──────┬──────────────┘
       │
       │ Student clicks "Generate Challan"
       ▼
┌─────────────────────────────────────┐
│ GSTFinanceLabService                │
│ .generateChallan(returnId)          │
└──────┬──────────────────────────────┘
       │
       │ Generate Mock CPIN
       ▼
┌─────────────────────────────────────┐
│ INSERT INTO gst_lab_challans        │
│ • challan_number: CPIN1234567890    │
│ • total_amount: ₹3,000              │
│ • status: 'GENERATED'               │
└──────┬──────────────────────────────┘
       │
       │ Challan Created
       ▼
┌─────────────────────┐
│  Pending Challan    │
│  Shows in Payment   │
│  Tab                │
└──────┬──────────────┘
       │
       │ Student clicks "Pay Now"
       ▼
┌─────────────────────────────────────┐
│ GSTFinanceLabService                │
│ .makePayment(challanId)             │
└──────┬──────────────────────────────┘
       │
       │ 1. Generate Transaction ID
       ▼
┌─────────────────────────────────────┐
│ transaction_id: TXN1707123456789    │
└──────┬──────────────────────────────┘
       │
       │ 2. Create Payment Record
       ▼
┌─────────────────────────────────────┐
│ INSERT INTO gst_lab_payments        │
│ • payment_amount: ₹3,000            │
│ • transaction_id: TXN...            │
└──────┬──────────────────────────────┘
       │
       │ 3. TRIGGER: Update Cash Ledger
       ▼
┌─────────────────────────────────────┐
│ INSERT INTO gst_lab_ledgers         │
│ • ledger_type: CASH_LEDGER          │
│ • debit_amount: ₹3,000              │
└──────┬──────────────────────────────┘
       │
       │ 4. Update Challan Status
       ▼
┌─────────────────────────────────────┐
│ UPDATE gst_lab_challans             │
│ SET status = 'PAID'                 │
└──────┬──────────────────────────────┘
       │
       │ 5. Update Return Status
       ▼
┌─────────────────────────────────────┐
│ UPDATE gst_lab_returns              │
│ SET status = 'PAID'                 │
└──────┬──────────────────────────────┘
       │
       │ Payment Complete
       ▼
┌─────────────────────┐
│  Payment Success    │
│  Cash Ledger: +₹3K  │
│  Return: PAID       │
└─────────────────────┘
```

---

## 🎯 FRONTEND TAB STRUCTURE

```
┌─────────────────────────────────────────────────────────────┐
│                    GST FINANCE LAB                          │
│                 Educational Simulation                       │
└─────────────────────────────────────────────────────────────┘
│
├── 📋 Setup Tab (One-time)
│   ├── Company Name Input
│   ├── State Selection
│   ├── GST Rate Selection
│   └── Register Button → Generates GSTIN
│
├── 📊 Dashboard Tab
│   ├── Ledger Balance Cards
│   │   ├── 🟢 Input Tax Credit (ITC)
│   │   ├── 🔴 Output GST Liability
│   │   └── 🔵 Net Payable
│   ├── Summary Statistics
│   │   ├── Total Purchases
│   │   ├── Total Sales
│   │   └── Returns Filed
│   └── Recent Transactions
│
├── 🛒 Purchase Tab
│   ├── Add Purchase Form
│   │   ├── Supplier Details
│   │   ├── Invoice Details
│   │   ├── Amount & GST Rate
│   │   └── Transaction Type
│   └── Recent Purchases List
│
├── 🧾 Sales Tab
│   ├── Add Sales Form
│   │   ├── Customer Details
│   │   ├── Invoice Details
│   │   ├── Amount & GST Rate
│   │   └── Transaction Type
│   └── Recent Sales List
│
├── 💰 Ledger Tab
│   ├── Electronic Credit Ledger (ITC)
│   │   └── Shows all ITC entries
│   ├── Electronic Liability Ledger
│   │   └── Shows all Output GST
│   └── Electronic Cash Ledger
│       └── Shows all payments
│
├── 📄 Return Tab
│   ├── Generate Return Form
│   │   ├── Month Selection
│   │   ├── Year Selection
│   │   └── Generate Button
│   └── Filed Returns List
│       ├── Return Summary
│       ├── File Return Button
│       └── Generate Challan Button
│
└── 💳 Payment Tab
    ├── Pending Challans
    │   ├── Challan Details
    │   ├── Amount Payable
    │   └── Pay Now Button
    └── Payment History
        └── Completed Payments
```

---

## 🔐 SECURITY & ISOLATION

```
┌─────────────────────────────────────┐
│  Student A                          │
│  allocation_id: 1                   │
└──────┬──────────────────────────────┘
       │
       │ Creates Company
       ▼
┌─────────────────────────────────────┐
│ gst_lab_companies                   │
│ • id: 1                             │
│ • allocation_id: 1 ◄────────────────┼─── Linked to Student A
│ • gstin: 33AAAAA1111A1Z5            │
└─────────────────────────────────────┘
       │
       │ All transactions
       ▼
┌─────────────────────────────────────┐
│ Purchases, Sales, Ledgers, Returns  │
│ All linked to company_id: 1         │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│  Student B                          │
│  allocation_id: 2                   │
└──────┬──────────────────────────────┘
       │
       │ Creates Company
       ▼
┌─────────────────────────────────────┐
│ gst_lab_companies                   │
│ • id: 2                             │
│ • allocation_id: 2 ◄────────────────┼─── Linked to Student B
│ • gstin: 33BBBBB2222B1Z5            │
└─────────────────────────────────────┘
       │
       │ All transactions
       ▼
┌─────────────────────────────────────┐
│ Purchases, Sales, Ledgers, Returns  │
│ All linked to company_id: 2         │
└─────────────────────────────────────┘

✅ Complete Data Isolation
✅ No cross-student data access
✅ Sandbox environment per student
```

---

## 📈 LEARNING PROGRESSION

```
Week 1: Company Setup & Basic Transactions
├── Register mock company
├── Add 5 purchases
├── Add 5 sales
└── View ledger balances

Week 2: Monthly Return Filing
├── Generate monthly return
├── Understand ITC vs Output GST
├── Calculate net tax payable
└── File return

Week 3: Payment Simulation
├── Generate payment challan
├── Make simulated payment
├── View cash ledger
└── Understand payment flow

Week 4: Advanced Scenarios
├── Inter-state transactions
├── ITC carry forward
├── Multiple months
└── Complete compliance cycle
```

---

## 🎓 EDUCATIONAL OUTCOMES

```
┌─────────────────────────────────────┐
│  Before GST Finance Lab             │
└─────────────────────────────────────┘
│
├── ❌ Theoretical knowledge only
├── ❌ No hands-on practice
├── ❌ Confusion about ITC
└── ❌ Fear of GST compliance

┌─────────────────────────────────────┐
│  After GST Finance Lab              │
└─────────────────────────────────────┘
│
├── ✅ Practical understanding
├── ✅ Hands-on experience
├── ✅ Clear ITC concept
├── ✅ Confidence in GST
├── ✅ Ready for real-world
└── ✅ Job-ready skills
```

---

**This architecture ensures a complete, isolated, and educational GST learning experience!** 🚀
